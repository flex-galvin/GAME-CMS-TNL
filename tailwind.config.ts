import type { Config } from 'tailwindcss'

export default <Partial<Config>>{
  theme: {
    fontFamily: {
      sans: [
        "'Lexend'", 
        "'ui-sans-serif'", 
        "'system-ui'", 
        "'sans-serif'",
        "'Apple Color Emoji'",
        "'Segoe UI Emoji'",
        "'Segoe UI Symbol'",
        "'Noto Color Emoji'",
      ],
    }
  }
}